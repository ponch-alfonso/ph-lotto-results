{
  "name": "daily-pcso",
  "scripts": {
    "serve": "npm run serve-functions && npm run run start-app",
    "serve-functions": "cd functions && npm run build && firebase emulators:start --project daily-pcso",
    "start-app": "cd app && npm run start"
  },
  "engines": {
    "node": "17"
  },
  "private": true,
  "devDependencies": {
    "@types/react": "^18.0.26",
    "@types/react-dom": "^18.0.10"
  }
}
